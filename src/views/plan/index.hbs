<h1>개발이야기</h1>
<div class="row">
    {{#each plans}}
        <div class="col s12 m4">
            <div class="card">
            <div class="card-image">
               {{{editIcon user ../user _id}}}
            </div>
            <div class="card-content center-align">
                <h5>{{title}}</h5>
                <p>{{stripTags (truncate body 250)}}</p>
                <br>
                <div class="chip">
                    <img src="{{user.image}}" alt="">
                    <a href="/plan/user/{{user._id}}">{{user.displayName}}</a>
                </div>
            </div>
            <div class="card-action center-align">
                <a href="/plan/{{_id}}" class="btn grey">Read More</a>
            </div>
            </div>
        </div>
    {{else}}
        <p>현재 프로젝트가 없습니다.</p>
    {{/each}}
</div>